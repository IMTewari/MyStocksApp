import { NextRequest, NextResponse } from "next/server"; import { KiteConnect } from "kiteconnect";
export const runtime = 'nodejs';
export async function GET(req: NextRequest) { const access = req.cookies.get("kite_access_token")?.value; if (!access) return NextResponse.json({ error: "not_authenticated" }, { status: 401 }); const u = new URL(req.url); const instruments = u.searchParams.getAll("i"); const kc = new KiteConnect({ api_key: process.env.KITE_API_KEY! }); kc.setAccessToken(access); const ltp = await kc.getLTP(instruments); return NextResponse.json(ltp); }